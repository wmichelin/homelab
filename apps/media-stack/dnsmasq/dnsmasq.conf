# Retired: *.g5.lan DNS moved to Headscale MagicDNS (docs/headscale-tailscale.md).
# Kept so an accidental compose reference fails closed instead of hijacking LAN DNS.
# Do not point eero Custom DNS here.
port=0

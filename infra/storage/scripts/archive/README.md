# Archived one-shot scripts

Historical G5 storage build / rename helpers. **Do not run** against the live array unless you know exactly why.

Live credentials and layout use `secrets/homelab.env` and walter/marissa Time Machine shares.

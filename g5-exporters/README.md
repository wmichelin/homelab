# Deprecated: use `apps/exporters/`

G5 exporters, SnapRAID textfile metrics, and related user units now live in [`apps/exporters/`](../apps/exporters/).

`./deploy-exporters-to-g5.sh` syncs from `apps/exporters/` into `~/code/homelab/apps/exporters` on the G5 (compat symlink: `~/homelab-exporters`).

The files in this directory are kept temporarily so old docs/links still resolve; do not edit them — change `apps/exporters/` instead.
